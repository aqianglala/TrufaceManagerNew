<local:MyWindow x:Class="TrufaceManager.View.AddJobPositionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrufaceManager"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        Title="AddJobPosition" Height="Auto" Width="Auto">
    <StackPanel Orientation="Vertical" Margin="16">
        <StackPanel Orientation="Horizontal">
            <TextBlock>Job Position Name</TextBlock>
            <TextBox Width="200" Margin="16,0,0,0" Text="{Binding Model.Name}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button x:Name="BtnOk" Click="BtnOk_Click">OK</Button>
            <Button x:Name="BtnCancel" Margin="16,0,0,0" Click="BtnCancel_Click">Cancel</Button>
        </StackPanel>
    </StackPanel>
</local:MyWindow>
